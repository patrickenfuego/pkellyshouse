import React from "react";
import { Container, Row, Col } from "react-bootstrap";

const Intro = () => {
  return (
    <div className="tab-container">
      <div className="inner-container">
        <h1>header in intro</h1>
        <p>some text in intro area</p>
      </div>
    </div>
  );
};

export default Intro;
